#include <stdio.h>
#include <conio.h>
#include <string.h>
int main()
{
	int i;
	char l;
	 char str;
	printf("Enter a string\n");
	scanf ("%s",&str);
	puts(str);
	//printf("%c\n",str);
	/**l=strlen(str);
	for(i=0; i>=str; i++)
	{
		printf("%c",i);
	}
	**/
	return 0;
}
